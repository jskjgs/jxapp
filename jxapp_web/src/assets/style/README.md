## 当前目录结构

> 顺序依照编辑器显示顺序（文件夹后面一定要加`/`）

```
|—— core/                 # 核心css
|     |—— _common.scss       # 公共类
|     |—— _element.scss      # element样式重置
|     |—— _layout.scss       # 布局类
|     |—— _normalize.scss    # normailize
|     |—— _reset.scss        # 全局重置样式
|     |—— _transition.scss   # 过渡类
|     |—— index.scss         # 核心css入口
|
|—— helpers/              # mixin、function等
|     |—— _mixins.scss       # mixins
|     |—— _function.scss     # function
|
|—— variables/            # css变量
|     |—— _size.scss         # 尺寸变量
|     |—— _theme.scss        # 主题类样式
|     |—— index.scss         # css变量入口
|
|—— font.scss             # 图标字体样式
|
|—— index.scss            # 全局样式入口
```

## 注意事项
